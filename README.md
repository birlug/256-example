Example solution for [256 Challenge](https://github.com/birlug/256challenge).

Embeds `challenge.b64` as `payload.b64`. Compress writes an empty file. Decompress dumps the payload. Valid, but a terrible score.
